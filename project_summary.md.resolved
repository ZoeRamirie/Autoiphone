# Autoiphone 项目开发总结报告

本报告汇总了 Autoiphone 项目重构开发的全过程，包括原始动机、技术选型、遇到的关键障碍及其底层解决思路，供后续复查与迭代参考。

---

## 一、 项目开发背景与目的

**核心目的**：利用大模型 (LLM) 和声音克隆 (TTS) 技术，将普通的 Android 手机改造为具备真人沟通能力的“AI 智能销售/客服”。

**关键需求**：
1.  **真人感交互**：通过 CosyVoice 零样本克隆技术，极速复刻话务员音色，并支持流式对话。
2.  **硬件级集成**：通过 USB 声卡将 AI 音频模拟为耳机信号注入手机，直接通过手机基带拨打电话。
3.  **高稳定性**：解决云端 ASR/TTS 认证频繁失效、硬件选择漂移等上个版本遗留的顽疾。

---

## 二、 技术框架说明

系统采用模块化、解耦的架构设计方案：

| 模块 | 技术选型 | 功能职责 |
| :--- | :--- | :--- |
| **通信中枢** | Python EventBus | 异步解耦 ASR、LLM 与 TTS，实现状态自动流转。 |
| **手机控制** | ADB (Android Debug Bridge) | 拨号监控、通话状态检测（mCallState 轮询）。 |
| **语音识别 (ASR)** | 阿里云实时语音转写 | 将用户侧（手机内）的模拟信号转为文字。 |
| **思考引擎 (LLM)** | Qwen-Turbo (OpenAI 协议) | 理解上下文并生成销售口语化回复。 |
| **语音合成 (TTS)** | 阿里云百炼 CosyVoice-V1 | 接收文本，实时合成克隆后的 Pcm 音频流。 |
| **底层驱动** | PyAudio / USB 声卡 | 负责本地硬件层面的音频实时录制与播放。 |

---

## 三、 核心问题及其解决思路 (核心逻辑)

在重构过程中，我们攻克了四个最具代表性的难题：

### 1. ASR Token 403 认证失效
*   **问题描述**：原版代码依赖 SDK 获取 Token，但在生产环境下 SDK 经常失效或无法安装，导致 ASR 鉴权失败。
*   **解决思路 (零依赖方案)**：
    - 抛弃官方包，手动实现了阿里云 OpenAPI 的 **RPC HMAC-SHA1 签名算法**。
    - 逻辑：代码直接拿 `AccessKey ID/Secret` 进行 Base64 签名生成时间戳，实时构造认证请求。
    - **收益**：不再受 SDK 环境限制，Token 永久自动刷新。

### 2. 系统环境与 SSL 兼容性冲突
*   **问题描述**：Mac 系统下 Python 默认缺少根证书，导致 WebSocket 连接 DashScope 时频繁报错。
*   **解决思路**：
    - **证书注入**：显式指定 `certifi` 的证书路径到环境变量 `SSL_CERT_FILE`。
    - **验证忽略**：在 WebSocket 层面强制关闭证书检查，确保通信通畅。

### 3. 音频硬件“乱跳”与静音
*   **问题描述**：系统启动后，有的版本录音采集的是 Mac 麦克风，有的版本放音是 Mac 喇叭。导致 AI 听不见声音，手机也收不到声音。
*   **解决思路 (索引强绑定)**：
    - **硬件测绘**：通过 `pyaudio.get_device_info` 遍历所有设备，确定 USB 声卡固定索引为 `0`。
    - **硬锁定**：在 `input_device_index` 和 `output_device_index` 参数中死锁 Index 0。
    - **采样率对齐**：全链路锁定 16000Hz (ASR) 或 22050Hz (TTS 默认)，消除硬件重采样造成的静音或爆音。

### 4. 碎句并行导致的“发声冲突”
*   **问题描述**：LLM 吐字很快，会连续发布多个 [ai_text_chunk](file:///Users/kellychen/CodeBuddy/Autoiphone/ai_caller.py#94-98) 事件，导致 TTS 启动多个并发连接，声卡被占用报错。
*   **解决思路 (串行锁机制)**：
    - 在 TTS 引擎内部引入 `threading.Lock()`。
    - **逻辑**：前一句话没播完，下一句话必须在 `with self._lock` 队列中等待，确保语音流连贯有序。

---

## 四、 硬件操作最佳实践 (备查)

1.  **母本上传**：系统目前采用 `uguu.se` 作为临时的母本中转站，确保百炼云端可稳定拉取 [.wav](file:///Users/kellychen/CodeBuddy/Autoiphone/core/audio/my_voice.wav)。
2.  **免提禁忌**：**绝对禁止开启免提**。开启免提会触发手机系统的 AEC (回声消除) 算法，导致线路输入的声音被当作回声强行切除。
3.  **ASR 状态探测**：通话之所以接通 10 秒即断，通常是因为 ASR 在前 10 秒没听到足够的声音（VAD 超时）。需配合增加麦克风增益或检查声卡输入模式。

---
*Autoiphone 项目复盘记录 v1.0 | 2026-03-15*
